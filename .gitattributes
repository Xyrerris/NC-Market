# Gli script del container devono avere line ending LF anche se clonati su Windows:
# uno shebang con CRLF fa fallire l'avvio con "no such file or directory".
docker/* text eol=lf
Dockerfile text eol=lf
*.sh text eol=lf
