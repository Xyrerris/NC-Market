bin/
obj/

# Database SQLite e file collegati (WAL, shm, lock, backup di migrazione)
*.db
*.db-wal
*.db-shm
*.db.lock
*.bak

# Esportazioni
*.csv

# Elenchi di productId grezzi, usati durante l'esplorazione iniziale dell'API
p0.txt
p1.txt

# IDE
.vs/
.idea/
*.user
bash.exe.stackdump

# Profili di avvio dell'IDE: è lì che vanno token e chat id per il debug locale, e un
# token di bot è una credenziale al portatore — questo file non entra nel repository.
**/Properties/launchSettings.json
